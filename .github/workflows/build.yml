name: publishing

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    permissions:
      id-token: write
    - uses: subosito/flutter-action@v2
      with:
        channel: 'stable'
        cache: true
    - uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1